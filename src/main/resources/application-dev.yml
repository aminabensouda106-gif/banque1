spring:
  datasource:
    url: jdbc:postgresql://localhost:5433/banque_agence
    username: banque
    password: banque
  jpa:
    hibernate:
      ddl-auto: validate
    open-in-view: false
  flyway:
    enabled: true
    locations: classpath:db/migration

logging:
  level:
    org.flywaydb: INFO
